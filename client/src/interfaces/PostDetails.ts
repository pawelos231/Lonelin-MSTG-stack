export interface PostDetails {
    title: string,
    username: string,
    image: any,
    createdat: string, 
    message: string,
}

export interface StatusLogin {
    text: string,
    status : number,
}